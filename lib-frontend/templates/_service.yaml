{{- define "frontend.service.tpl" -}}
kind: Service
apiVersion: v1
metadata:
  name: {{ .Chart.Name }}-frontend
spec:
  selector:
    app: {{ .Chart.Name }}-frontend
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
{{- end -}}
